meta {
  name: refresh_token
  type: http
  seq: 8
}

post {
  url: http://127.0.0.1:5000/auth/refresh
  body: json
  auth: bearer
}

auth:bearer {
  token: {{Access_Token_Achal}}
}

body:json {
  {
    "refresh_token": "{{Refresh_Token_Achal}}"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
