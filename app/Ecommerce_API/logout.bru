meta {
  name: logout
  type: http
  seq: 3
}

post {
  url: http://127.0.0.1:5000/auth/logout
  body: json
  auth: bearer
}

auth:bearer {
  token: {{Access_Token_Achal}}
}

body:json {
  {
    "refresh_token": "{{Refresh_Token_Achal}}"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
